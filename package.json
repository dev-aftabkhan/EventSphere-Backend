{
  "name": "nexio-eventsphere",
  "version": "1.0.0",
  "description": "```\r /config       -> Configuration files (DB, Kafka, etc.)\r /consumers    -> Kafka consumers handling message processing\r /controllers  -> API controllers managing request logic\r /database     -> Database connections and models (MySQL & MongoDB)\r /kafka        -> Kafka-related configurations and utilities\r /middleware   -> Middleware for authentication, logging, validation, etc.\r /producers    -> Kafka producers for publishing messages\r /routes       -> API routes (Express)\r /services     -> Business logic layer\r /utils        -> Utility files (logging, helpers, etc.)\r /webhooks     -> discord webhook's\r .env          -> For env var\r server.js     -> Main entry point for the application\r ```",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "axios": "^1.8.2",
    "bcrypt": "^5.1.1",
    "bcryptjs": "^3.0.2",
    "chalk": "^4.1.2",
    "compression": "^1.8.0",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "express-fileupload": "^1.5.1",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.2",
    "kafka-node": "^5.0.0",
    "kafkajs": "^2.2.4",
    "moment": "^2.30.1",
    "mongoose": "^8.12.1",
    "morgan": "^1.10.0",
    "multer": "^1.4.5-lts.1",
    "mysql2": "^3.13.0"
  }
}
